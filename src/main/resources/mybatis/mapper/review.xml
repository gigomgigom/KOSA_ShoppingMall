<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.javajavajo.dao.ReviewDao">
	<insert id="insert" parameterType="Review">
		insert into review (ordno, prodno, rvtitle, rvdate, rvcontent, rvrating)
		values(#{ordno}, #{prodno}, #{rvtitle}, sysdate, #{rvcontent}, #{rvrating})
	</insert>
	
	<select id="count" resultType="int">
		select count(*) from review
		where ordno = #{ordno} and prodno = #{prodno}
	</select>
</mapper>